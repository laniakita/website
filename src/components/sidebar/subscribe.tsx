import {
	SubscribeBox,
	type SubscribeBoxProps,
} from "$/src/stories/sidebar/subscribe-box";

export function SidebarSubscribe({ className }: SubscribeBoxProps) {
	return <SubscribeBox className={className} />;
}
