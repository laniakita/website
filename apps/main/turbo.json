{
  "$schema": "https://turborepo.dev/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": [
        "dist/**",
        ".output/**"
      ],
      "env": [
        "CLOUDFLARE_API_TOKEN"
      ],
      "passThroughEnv": [
        "CLOUDFLARE_API_TOKEN"
      ],
      "inputs": [
        "$TURBO_DEFAULT$",
        ".env*"
      ]
    },
    "preview": {
      "dependsOn": ["build"]
    },
    "build-storybook": {
      "outputs": [
        "storybook-static/**"
      ],
      "inputs": [
        "$TURBO_DEFAULT$",
        ".env",
        ".env*"
      ]
    },
    "deploy:preview": {
      "env": ["CLOUDFLARE_API_TOKEN"],
      "passThroughEnv": [
        "CLOUDFLARE_API_TOKEN"
      ],
    },
    "deploy": {
      "env": ["CLOUDFLARE_API_TOKEN"],
      "passThroughEnv": [
        "CLOUDFLARE_API_TOKEN"
      ],
    }
  }
}
