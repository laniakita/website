{
  "extends": ["//"],
  "globalEnv": [".env"],
  "tasks": {
    "analyze": {
      "env": ["ANALYZE"]
    },
    "pretty": {},
    "test": {},
    "build": {
      "env": ["NODE_ENV"],
      "outputs": [".next/**", "!.next/cache/**", "public/dist/**", "public/sw.js", ".contentlayer", ".contentlayermini"],
      "inputs": ["$TURBO_DEFAULT$", ".env.production.local", ".env.production", ".env.local", ".env"]
    },
    "dev": {
      "env": ["NODE_ENV"],
      "inputs": ["$TURBO_DEFAULT$", ".env.development.local", ".env.development", ".env.local", ".env"]
    },
    "start": {}
  }
}
